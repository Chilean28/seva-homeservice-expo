export enum UserType {
  CUSTOMER = 'customer',
  WORKER = 'worker',
}

export enum BookingStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  ONGOING = 'ongoing',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
